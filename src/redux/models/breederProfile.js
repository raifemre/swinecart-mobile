import to from 'await-to-js';

import {
  AuthService, NavigationService
} from 'services';

export default {
  state: {
    loading: false,
    profile: null
  },
  reducers: {

  },
  effects: (dispatch) => ({
    async getProfileData() {

    },
  })
}